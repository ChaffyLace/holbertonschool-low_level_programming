#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
char *_strpbrk(char *s, char *accept)
{
	while(*s != '\0')
	{
		while(*a != '\0')
		{
			if(*s == *a)
		}
	}
	return (NULL);
}
