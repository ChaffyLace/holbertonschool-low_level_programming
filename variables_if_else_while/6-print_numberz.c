#include <stdio.h>
#include <stdlib.h>
/**
 * main - Prints numbers.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	i == num;

	for (num = 0; num <= 9)
	{
		putchar(i + "0");
	}
	putchar('\n');
	return (0);
}
